<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:WpfResources.ProjectResources">

    <ControlTemplate TargetType="Button" x:Key="DarkBtn">
        <Border Background="DarkOliveGreen">
            <ContentControl Foreground="White"
                            Content="{TemplateBinding Content}"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center"
                            FontSize="18"></ContentControl>
        </Border>
    </ControlTemplate>

    <ControlTemplate TargetType="Label" x:Key="LabelKey2">
        <Border>
            <ContentControl Content="{TemplateBinding Content}" 
                            FontFamily="Italic"></ContentControl>
        </Border>
    </ControlTemplate>

    <Style TargetType="Grid">
        <Setter Property="Background" Value="AntiqueWhite"></Setter>
    </Style>
    <ControlTemplate TargetType="TextBox" x:Key="TextBoxTemplate2">
        <Border Background="Gainsboro">
            <ContentControl></ContentControl>
        </Border>
    </ControlTemplate>

</ResourceDictionary>
